export type FocusArea = "sidebar" | "category" | "games" | "details";

export type InputMethod = "gamepad" | "mouse" | "keyboard";

/** Favorite star, Copy path, Launch — must match GameDetailsSidebar focus order */
export const DETAILS_FOCUS_MAX_INDEX = 2;
