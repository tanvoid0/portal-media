export type FocusArea = "sidebar" | "category" | "games" | "details";

export type InputMethod = "gamepad" | "mouse" | "keyboard";

/** Favourite, Copy path, Open in folder, Launch, Archive, category Game/App/Media — must match GameDetailsContent focus order */
export const DETAILS_FOCUS_MAX_INDEX = 7;
